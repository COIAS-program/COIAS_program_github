#!/bin/bash
# Timestamp: 2023/01/13 15:30 sugiura
####################################################################
# カレントディレクトリにあるfits, edb, pngファイルを削除する.
# このスクリプトはpostprocessスクリプトから呼び出される他,
# ウェブCOIASの右上の「探索終了」ボタンを押した時にも呼び出されるようにしたい.
####################################################################

rm -f ./*.fits
rm -f ./*.edb
rm -f ./*.png
